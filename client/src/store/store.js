import Vue from 'vue';
import Vuex from 'vuex';
import axios from 'axios';

Vue.use(Vuex);

const state = {
};

const getters = {

};

const actions = {

};

const mutations = {

};

export const store = new Vuex.Store({
  modules: {
    state,
    getters,
    actions,
    mutations,
  }
});
